[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn whatsapp_bot:app --bind 0.0.0.0:$PORT --workers 1 --timeout 60"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"